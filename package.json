{
  "name": "Petrus",
  "description": "A Piet compiler",
  "repository": {
    "type": "git",
    "url": "https://github.com/mizkichan/petrus"
  },
  "scripts": {
    "start": "parcel serve src/index.html",
    "build": "parcel build src/index.html",
    "clean": "rm -rf dist",
    "gh-pages": "npm run clean && npm run build -- --public-url . --no-source-maps && gh-pages -d dist"
  },
  "devDependencies": {
    "elm-hot": "^1.1.2",
    "gh-pages": "^2.1.1",
    "node-elm-compiler": "^5.0.4",
    "parcel-bundler": "^1.12.3",
    "sass": "^1.22.10"
  },
  "dependencies": {
    "bulma": "^0.7.5"
  },
  "private": true
}
